[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "researchdoc"
version = "0.1.0"
description = "ResearchDoc-QA"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]